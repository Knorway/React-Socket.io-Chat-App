import { extendTheme } from '@chakra-ui/react';

export const theme = extendTheme({
	styles: {
		global: {
			body: {
				fontFamily:
					'Noto Sans KR, -apple-system, system-ui, Helvetica Neue, sans-serif',
			},
		},
	},
});
